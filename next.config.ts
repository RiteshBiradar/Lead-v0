/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['snappyui.in'],
  },
  // next.config.js
  eslint: {
    ignoreDuringBuilds: true,
  },

};

module.exports = nextConfig;
